<%= form_with(model: website, local: true, class: "needs-validation") do |form| %>
  <% if website.errors.any? %>
    <div class="alert alert-danger">
      <h4 class="alert-heading"><%= pluralize(website.errors.count, "error") %> prohibited this website from being saved:</h4>
      <ul class="mb-0">
        <% website.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :url, class: "form-label" %>
    <%= form.text_field :url, class: "form-control", placeholder: "https://example.com" %>
  </div>

  <div>
    <%= form.submit "Save Website", class: "btn btn-primary" %>
  </div>
<% end %>
